diff --git a/apps/pfotentechnik/src/layouts/SeoAdminLayout.astro b/apps/pfotentechnik/src/layouts/SeoAdminLayout.astro
index eea10bd..d4b07ee 100644
--- a/apps/pfotentechnik/src/layouts/SeoAdminLayout.astro
+++ b/apps/pfotentechnik/src/layouts/SeoAdminLayout.astro
@@ -145,5 +145,236 @@ const nav = [
         localStorage.setItem("pfotentechnik-seo-theme", next);
       });
     </script>
-  </body>
-</html>
+
+<script is:inline data-pt-editorial-theme-advisor-7-2>
+  (() => {
+    const norm = (v = "") =>
+      v.replace(/\s+/g, " ").trim().toLocaleLowerCase("de");
+
+    const rgb = (value) => {
+      const match = value?.match(
+        /rgba?\(\s*([\d.]+)[,\s]+([\d.]+)[,\s]+([\d.]+)(?:[,\s/]+([\d.]+))?\s*\)/
+      );
+      return match
+        ? {
+            r: Number(match[1]),
+            g: Number(match[2]),
+            b: Number(match[3]),
+            a: match[4] === undefined ? 1 : Number(match[4])
+          }
+        : null;
+    };
+
+    const channel = (v) => {
+      const n = v / 255;
+      return n <= 0.03928
+        ? n / 12.92
+        : ((n + 0.055) / 1.055) ** 2.4;
+    };
+
+    const lum = (c) =>
+      c
+        ? 0.2126 * channel(c.r) +
+          0.7152 * channel(c.g) +
+          0.0722 * channel(c.b)
+        : null;
+
+    const ratio = (a, b) => {
+      const first = lum(a);
+      const second = lum(b);
+      if (first === null || second === null) return 21;
+      return (Math.max(first, second) + 0.05) /
+        (Math.min(first, second) + 0.05);
+    };
+
+    const background = (element) => {
+      let current = element;
+      while (current && current !== document.documentElement) {
+        const color = rgb(getComputedStyle(current).backgroundColor);
+        if (color && color.a > 0.08) return color;
+        current = current.parentElement;
+      }
+      return rgb(getComputedStyle(document.body).backgroundColor);
+    };
+
+    const panelFor = (heading) => {
+      let current = heading.parentElement;
+      let fallback = null;
+
+      for (let depth = 0; current && depth < 7; depth += 1) {
+        if (
+          current.matches?.(
+            "section,article,aside,[class*='panel'],[class*='card'],[class*='method'],[class*='proof'],[class*='trust'],[class*='position']"
+          )
+        ) {
+          fallback ||= current;
+          const style = getComputedStyle(current);
+          const radius = Number.parseFloat(style.borderRadius) || 0;
+          const color = rgb(style.backgroundColor);
+          if (radius >= 12 && color && color.a > 0.08) return current;
+        }
+        current = current.parentElement;
+      }
+
+      return fallback ||
+        heading.closest("section,article,aside") ||
+        heading.parentElement;
+    };
+
+    const classifyKnownPanels = () => {
+      const rules = [
+        ["empfehlungen brauchen eine Ã¼berprÃ¼fbare grundlage", "pt-method-panel"],
+        ["orientierung statt produktwerbung", "pt-proof-panel"],
+        ["kein shop. keine herstellerplattform.", "pt-positioning-panel"],
+        ["transparenz vor der produktauswahl", "pt-transparency-faq"]
+      ];
+
+      document
+        .querySelectorAll("main h1,main h2,main h3,main h4")
+        .forEach((heading) => {
+          const text = norm(heading.textContent);
+          const rule = rules.find(([phrase]) => text.includes(phrase));
+          if (!rule) return;
+
+          const panel = panelFor(heading);
+          panel?.classList.add(rule[1]);
+
+          if (rule[1] !== "pt-transparency-faq") {
+            panel?.classList.add("pt-editorial-dark-panel");
+          }
+        });
+    };
+
+    const fixLowContrast = () => {
+      document
+        .querySelectorAll(
+          "main section,main article,main aside,main details,main [class*='card'],main [class*='panel']"
+        )
+        .forEach((element) => {
+          if (
+            element.classList.contains("pt-editorial-dark-panel") ||
+            element.classList.contains("pt-transparency-faq")
+          ) return;
+
+          const heading = element.querySelector(
+            ":scope>h1,:scope>h2,:scope>h3,:scope>header h2,:scope>header h3"
+          );
+          const copy = element.querySelector(
+            ":scope>p,:scope>div>p,:scope li"
+          );
+          if (!heading && !copy) return;
+
+          const bg = background(element);
+          const bgLum = lum(bg);
+          if (bgLum === null) return;
+
+          const headingColor = heading
+            ? rgb(getComputedStyle(heading).color)
+            : null;
+          const copyColor = copy
+            ? rgb(getComputedStyle(copy).color)
+            : null;
+
+          const bad =
+            (headingColor && ratio(bg, headingColor) < 3.2) ||
+            (copyColor && ratio(bg, copyColor) < 4.05);
+
+          if (!bad) return;
+
+          if (bgLum < 0.24) {
+            element.classList.add("pt-auto-dark-panel");
+          } else if (bgLum > 0.72) {
+            element.classList.add("pt-auto-light-panel");
+          }
+        });
+    };
+
+    const configureAdvisor = () => {
+      let section = document.querySelector(
+        "#kaufberatung,.home41-decision"
+      );
+
+      if (!section) {
+        const heading = [...document.querySelectorAll("main h1,main h2,main h3")]
+          .find((element) =>
+            norm(element.textContent).includes(
+              "direkt zum passenden vergleich"
+            )
+          );
+        section = heading?.closest("section,article,div");
+      }
+
+      if (section) section.id = "kaufberatung";
+
+      document
+        .querySelectorAll("header a,header button,nav a")
+        .forEach((element) => {
+          const text = norm(element.textContent);
+          const href = norm(element.getAttribute?.("href") || "");
+          const advisor =
+            text.includes("berater") ||
+            text.includes("kaufberatung") ||
+            href.includes("berater") ||
+            href.includes("advisor");
+
+          if (!advisor) return;
+
+          if (element.tagName === "A") {
+            element.setAttribute("href", "/#kaufberatung");
+          }
+
+          const textNode = [...element.childNodes].find(
+            (node) =>
+              node.nodeType === Node.TEXT_NODE &&
+              norm(node.textContent)
+          );
+
+          if (textNode) {
+            textNode.textContent = "Kaufberatung";
+          } else if (!element.querySelector("svg,img")) {
+            element.textContent = "Kaufberatung";
+          }
+
+          element.classList.add("pt-nav-advisor-cta");
+          element.setAttribute(
+            "aria-label",
+            "Kaufberatung fÃ¼r Futterautomaten, Trinkbrunnen und GPS-Tracker"
+          );
+          element.setAttribute(
+            "title",
+            "Kaufberatung fÃ¼r Futterautomaten, Trinkbrunnen und GPS-Tracker"
+          );
+        });
+
+      if (location.hash === "#kaufberatung" && section) {
+        requestAnimationFrame(() =>
+          section.scrollIntoView({
+            behavior: "smooth",
+            block: "start"
+          })
+        );
+      }
+    };
+
+    const apply = () => {
+      classifyKnownPanels();
+      fixLowContrast();
+      configureAdvisor();
+    };
+
+    if (document.readyState === "loading") {
+      document.addEventListener(
+        "DOMContentLoaded",
+        apply,
+        { once: true }
+      );
+    } else {
+      apply();
+    }
+
+    document.addEventListener("astro:page-load", apply);
+    setTimeout(apply, 350);
+  })();
+</script>
+</body>
+</html>
\ No newline at end of file
diff --git a/apps/pfotentechnik/src/styles/pfotentechnik-design-system.css b/apps/pfotentechnik/src/styles/pfotentechnik-design-system.css
index af930ca..d90d5b5 100644
--- a/apps/pfotentechnik/src/styles/pfotentechnik-design-system.css
+++ b/apps/pfotentechnik/src/styles/pfotentechnik-design-system.css
@@ -6247,3 +6247,208 @@ body:has(.premium-v3--pfotentechnik){padding-bottom:calc(88px + env(safe-area-in
   box-shadow: none !important;
 }
 /* End PT product design system 2.1 / 7.1 */
+
+/* PT editorial theme + advisor UX 2.2 / 7.2 */
+:root {
+  --pt-dark-bg:#07372f;
+  --pt-dark-border:rgba(151,227,185,.22);
+  --pt-dark-heading:#f4fbf7;
+  --pt-dark-copy:#c6d8d0;
+  --pt-dark-accent:#8ce9b2;
+  --pt-light-bg:#fff;
+  --pt-light-border:#dfe7e4;
+  --pt-light-heading:#101827;
+  --pt-light-copy:#4d5b6d;
+  --pt-accent:#208b50;
+}
+
+:is(.pt-editorial-dark-panel,.pt-method-panel,.pt-proof-panel,.pt-positioning-panel,.pt-auto-dark-panel){
+  color:var(--pt-dark-copy);
+  border-color:var(--pt-dark-border)!important;
+}
+:is(.pt-editorial-dark-panel,.pt-method-panel,.pt-proof-panel,.pt-positioning-panel){
+  background:
+    radial-gradient(circle at 100% 0,rgba(88,216,143,.08),transparent 34%),
+    var(--pt-dark-bg)!important;
+}
+:is(.pt-editorial-dark-panel,.pt-method-panel,.pt-proof-panel,.pt-positioning-panel,.pt-auto-dark-panel)
+:is(h1,h2,h3,h4,h5,h6,strong){
+  color:var(--pt-dark-heading)!important;
+}
+:is(.pt-editorial-dark-panel,.pt-method-panel,.pt-proof-panel,.pt-positioning-panel,.pt-auto-dark-panel)
+:is(p,li,dd,small,figcaption){
+  color:var(--pt-dark-copy)!important;
+}
+:is(.pt-editorial-dark-panel,.pt-method-panel,.pt-proof-panel,.pt-positioning-panel,.pt-auto-dark-panel)
+:is([class*="eyebrow"],[class*="kicker"],[class*="label"],[class*="index"],[class*="number"]){
+  color:var(--pt-dark-accent)!important;
+}
+:is(.pt-editorial-dark-panel,.pt-method-panel,.pt-proof-panel,.pt-positioning-panel,.pt-auto-dark-panel) a{
+  color:var(--pt-dark-accent)!important;
+  text-decoration-color:color-mix(in srgb,var(--pt-dark-accent) 55%,transparent);
+  text-underline-offset:.18em;
+}
+.pt-auto-light-panel :is(h1,h2,h3,h4,h5,h6,strong){
+  color:var(--pt-light-heading)!important;
+}
+.pt-auto-light-panel :is(p,li,dd,small,figcaption){
+  color:var(--pt-light-copy)!important;
+}
+
+.pt-method-panel,
+.pt-proof-panel,
+.pt-positioning-panel{
+  padding:clamp(1.45rem,5vw,2.5rem)!important;
+}
+.pt-method-panel>:is(h1,h2,h3),
+.pt-positioning-panel>:is(h1,h2,h3){
+  max-width:13ch;
+  margin-bottom:1rem;
+  font-size:clamp(2rem,7vw,3.6rem);
+  line-height:.98;
+  letter-spacing:-.052em;
+  text-wrap:balance;
+}
+.pt-proof-panel>:is(h1,h2,h3){
+  max-width:14ch;
+  font-size:clamp(2rem,6.5vw,3.45rem);
+  line-height:1;
+  letter-spacing:-.048em;
+  text-wrap:balance;
+}
+.pt-method-panel :is(ol,ul)>li{
+  border-color:rgba(218,238,228,.16)!important;
+}
+.pt-method-panel :is(ol,ul)>li :is(h3,h4,strong){
+  color:var(--pt-dark-heading)!important;
+}
+.pt-proof-panel :is([class*="stats"],[class*="metrics"],[class*="numbers"]){
+  display:grid;
+  gap:.75rem;
+  margin-top:1.35rem;
+}
+.pt-proof-panel :is([class*="stat"],[class*="metric"]){
+  min-width:0;
+  border:1px solid rgba(211,238,225,.18)!important;
+  border-radius:1rem;
+  background:rgba(255,255,255,.045)!important;
+}
+.pt-proof-panel :is([class*="stat"],[class*="metric"])
+:is(strong,b,[class*="value"],[class*="number"]){
+  color:var(--pt-dark-accent)!important;
+  font-variant-numeric:tabular-nums;
+}
+.pt-positioning-panel :is(p,li){max-width:38ch}
+
+.pt-transparency-faq{
+  padding-inline:0!important;
+  border:0!important;
+  color:var(--pt-light-heading);
+  background:transparent!important;
+  box-shadow:none!important;
+}
+.pt-transparency-faq>:is(h1,h2,h3),
+.pt-transparency-faq>:is(header,div):first-child :is(h1,h2,h3){
+  max-width:16ch;
+  color:var(--pt-light-heading)!important;
+  font-size:clamp(2rem,7vw,3.25rem);
+  line-height:1;
+  letter-spacing:-.045em;
+  text-wrap:balance;
+}
+.pt-transparency-faq>:is(p,header p,div:first-child p){
+  max-width:46ch;
+  color:var(--pt-light-copy)!important;
+}
+.pt-transparency-faq :is(details,[class*="faq-item"],[class*="accordion-item"]){
+  overflow:hidden;
+  border:1px solid var(--pt-light-border)!important;
+  border-radius:1rem!important;
+  color:var(--pt-light-heading);
+  background:var(--pt-light-bg)!important;
+  box-shadow:0 8px 24px rgba(20,33,28,.045);
+}
+.pt-transparency-faq :is(summary,[class*="question"],[class*="trigger"]){
+  min-height:58px;
+  color:var(--pt-light-heading)!important;
+  background:transparent!important;
+}
+.pt-transparency-faq :is(details p,[class*="answer"],[class*="content"]){
+  color:var(--pt-light-copy)!important;
+}
+
+#kaufberatung{scroll-margin-top:7rem}
+.pt-nav-advisor-cta{
+  display:inline-flex!important;
+  min-height:42px;
+  align-items:center;
+  justify-content:center;
+  gap:.45rem;
+  padding:.68rem .95rem!important;
+  border:1px solid rgba(32,139,80,.35)!important;
+  border-radius:999px!important;
+  color:#071811!important;
+  background:#5cdf91!important;
+  font-weight:850!important;
+  text-decoration:none!important;
+  box-shadow:0 8px 20px rgba(28,151,80,.15);
+  transition:transform 150ms ease,background 150ms ease,box-shadow 150ms ease;
+}
+.pt-nav-advisor-cta::after{content:"â†’";font-size:.95em}
+.pt-nav-advisor-cta:hover{
+  transform:translateY(-1px);
+  background:#72e7a2!important;
+  box-shadow:0 12px 26px rgba(28,151,80,.2);
+}
+.pt-nav-advisor-cta:focus-visible{
+  outline:3px solid rgba(70,205,127,.34);
+  outline-offset:3px;
+}
+
+@media (max-width:720px){
+  :is(.pt-method-panel,.pt-proof-panel,.pt-positioning-panel){
+    width:min(100%,calc(100vw - 2rem));
+    margin-inline:auto!important;
+    border-radius:1.35rem!important;
+  }
+  .pt-transparency-faq{
+    width:min(100%,calc(100vw - 2rem));
+    margin-inline:auto!important;
+  }
+}
+
+@media (prefers-color-scheme:dark){
+  :root{
+    --pt-light-bg:#101d2f;
+    --pt-light-border:rgba(203,218,232,.14);
+    --pt-light-heading:#f4f8fb;
+    --pt-light-copy:#b8c6d4;
+    --pt-accent:#5cdf91;
+  }
+  .pt-transparency-faq :is(details,[class*="faq-item"],[class*="accordion-item"]){
+    box-shadow:none;
+  }
+  :is(.pt-editorial-dark-panel,.pt-method-panel,.pt-proof-panel,.pt-positioning-panel){
+    border-color:rgba(131,225,174,.2)!important;
+    background:
+      radial-gradient(circle at 100% 0,rgba(88,216,143,.075),transparent 34%),
+      #0b302a!important;
+    box-shadow:none!important;
+  }
+}
+:is(html.dark,html[data-theme="dark"],html[data-color-scheme="dark"],body.dark,[data-theme="dark"]){
+  --pt-light-bg:#101d2f;
+  --pt-light-border:rgba(203,218,232,.14);
+  --pt-light-heading:#f4f8fb;
+  --pt-light-copy:#b8c6d4;
+  --pt-accent:#5cdf91;
+}
+:is(html.dark,html[data-theme="dark"],html[data-color-scheme="dark"],body.dark,[data-theme="dark"])
+:is(.pt-editorial-dark-panel,.pt-method-panel,.pt-proof-panel,.pt-positioning-panel){
+  border-color:rgba(131,225,174,.2)!important;
+  background:
+    radial-gradient(circle at 100% 0,rgba(88,216,143,.075),transparent 34%),
+    #0b302a!important;
+  box-shadow:none!important;
+}
+/* End PT editorial theme + advisor UX 2.2 / 7.2 */
