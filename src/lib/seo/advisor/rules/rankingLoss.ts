import type { AdvisorRule } from "../types";
import { calculateScore, priorityForScore } from "../score";
export const rankingLossRule:AdvisorRule={id:"ranking-loss",label:"Ranking-Verluste",category:"ranking",run(context){return context.pages.flatMap((page)=>{const m=page.metric;if(!m||m.previousPosition==null||m.position==null||(m.impressions??0)<50)return[];const loss=m.position-m.previousPosition;if(loss<2)return[];const impact=Math.min(100,58+loss*5+Math.log10((m.impressions??1)+1)*6);const confidence=84;const effort="medium" as const;const score=calculateScore(impact,confidence,effort);return[{id:`ranking-loss:${page.url}`,ruleId:"ranking-loss",category:"ranking" as const,title:"Ranking-Verlust prüfen",summary:`${page.title??page.url} verlor rund ${loss.toFixed(1)} Positionen.`,reason:"Ein deutlicher Positionsverlust kann auf veränderte Suchintention, stärkere Konkurrenz, technische Probleme oder Aktualisierungsbedarf hinweisen.",action:"Suchanfragen, SERP-Intent, Content-Abdeckung, Aktualität, interne Links und Indexierung prüfen.",url:page.url,impact,confidence,effort,score,priority:priorityForScore(score),evidence:{previousPosition:Number(m.previousPosition.toFixed(1)),position:Number(m.position.toFixed(1)),loss:Number(loss.toFixed(1)),impressions:m.impressions??0}}];});}};
