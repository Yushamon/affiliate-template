import type { AdvisorRule } from "../types";
import { calculateScore, priorityForScore } from "../score";
export const internalLinksRule:AdvisorRule={id:"internal-links",label:"Interne Links",category:"internal-links",run(context){return context.pages.flatMap((page)=>{if(page.inboundLinks==null||page.inboundLinks>1)return[];const impressions=page.metric?.impressions??0;const impact=Math.min(96,62+Math.log10(impressions+1)*8);const confidence=92;const effort="low" as const;const score=calculateScore(impact,confidence,effort);return[{id:`internal-links:${page.url}`,ruleId:"internal-links",category:"internal-links" as const,title:page.inboundLinks===0?"Verwaiste Seite anbinden":"Zu wenige interne Links",summary:`${page.title??page.url} erhält aktuell nur ${page.inboundLinks} eingehende interne Links.`,reason:"Die interne Linkautorität und Auffindbarkeit sind zu schwach.",action:"Aus Cornerstone, passendem Ratgeber, Vergleich und mindestens einer Produkt- oder Herstellerseite verlinken.",url:page.url,impact,confidence,effort,score,priority:priorityForScore(score),evidence:{inboundLinks:page.inboundLinks,impressions}}];});}};
