import type { AdvisorRule } from "../types";
import { calculateScore, priorityForScore } from "../score";
export const thinContentRule:AdvisorRule={id:"thin-content",label:"Thin Content",category:"content",run(context){return context.pages.flatMap((page)=>{if(page.wordCount==null||page.wordCount>=650||(page.metric?.impressions??0)<30)return[];const impact=Math.min(88,52+Math.log10((page.metric?.impressions??0)+1)*10);const confidence=72;const effort="medium" as const;const score=calculateScore(impact,confidence,effort);return[{id:`thin-content:${page.url}`,ruleId:"thin-content",category:"content" as const,title:"Inhaltstiefe prüfen",summary:`${page.title??page.url} umfasst nur etwa ${page.wordCount} Wörter.`,reason:"Für die vorhandene Sichtbarkeit könnte die Seite Suchintention und Unterfragen noch nicht ausreichend abdecken.",action:"Keine künstliche Verlängerung: fehlende Entscheidungsfragen, Tabellen, Quellen, FAQ und interne Übergänge ergänzen.",url:page.url,impact,confidence,effort,score,priority:priorityForScore(score),evidence:{wordCount:page.wordCount,impressions:page.metric?.impressions??0}}];});}};
