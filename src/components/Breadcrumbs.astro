---
interface Props {
  categoryLabel: string;
  title: string;
}

const { categoryLabel, title } = Astro.props;
---

<nav class="breadcrumbs" aria-label="Breadcrumb">
  <a href="/">Startseite</a>
  <span>›</span>
  <a href="/balkonspeicher">{categoryLabel}</a>
  <span>›</span>
  <span>{title}</span>
</nav>