---
import Callout from "./ui/Callout.astro";

interface Props {
  title: string;
  text: string;
  url: string;
}

const { title, text, url } = Astro.props;
---

<Callout
  title={title}
  text={text}
  href={url}
  cta="Zum Vergleich"
/>