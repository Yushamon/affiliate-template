#!/usr/bin/env node
import fs from "node:fs/promises";import path from "node:path";import {fileURLToPath} from "node:url";import {mediaAudit} from "../../src/lib/media-center/service.mjs";const appRoot=path.resolve(fileURLToPath(new URL("../..",import.meta.url)));const report=await mediaAudit();await fs.mkdir(path.join(appRoot,"reports"),{recursive:true});await fs.writeFile(path.join(appRoot,"reports","media-center-audit.json"),JSON.stringify(report,null,2));console.log(`Media Center: ${report.summary.jobs} Jobs, ${report.summary.approved} freigegeben, ${report.summary.rejectedImages} Bilder verworfen.`);
