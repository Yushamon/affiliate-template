#!/usr/bin/env node

import fs from "node:fs";
import path from "node:path";
import process from "node:process";
import { fileURLToPath } from "node:url";
import { spawnSync } from "node:child_process";

const PATCH = "pfotentechnik-navigation-footer-compact-repair-32.2.1";
const SCRIPT_DIR = path.dirname(fileURLToPath(import.meta.url));
const skipBuild = process.argv.includes("--skip-build");

const candidates = [
  process.cwd(),
  path.resolve(SCRIPT_DIR, ".."),
  path.resolve(SCRIPT_DIR, "../..")
];

const root = candidates.find((candidate) =>
  fs.existsSync(path.join(candidate, "apps/pfotentechnik")) &&
  fs.existsSync(path.join(candidate, "packages/affiliate-core"))
);

if (!root) {
  throw new Error(`[${PATCH}] Repository-Root nicht gefunden.`);
}

const payloads = {
  "packages/affiliate-core/src/components/Header.astro": "---\ninterface HeaderLink {\n  label: string;\n  href: string;\n  mobileGroup?: \"Orientierung\" | \"Produktwelten\" | \"Mehr entdecken\";\n  mobileEmphasis?: boolean;\n  description?: string;\n  navCode?: string;\n}\n\ninterface Props {\n  projectName: string;\n  links: HeaderLink[];\n}\n\nconst { projectName, links } = Astro.props as Props;\n\nconst normalizePath = (value: string) => {\n  const pathname = String(value || \"/\").split(/[?#]/, 1)[0] || \"/\";\n  const withLeadingSlash = pathname.startsWith(\"/\") ? pathname : `/${pathname}`;\n  return withLeadingSlash === \"/\" || withLeadingSlash.endsWith(\"/\")\n    ? withLeadingSlash\n    : `${withLeadingSlash}/`;\n};\n\nconst currentPath = normalizePath(Astro.url.pathname);\nconst isCurrent = (href: string) => {\n  const normalizedHref = normalizePath(href);\n  return normalizedHref === \"/\"\n    ? currentPath === \"/\"\n    : currentPath === normalizedHref || currentPath.startsWith(normalizedHref);\n};\n\nconst groupOrder = [\"Orientierung\", \"Produktwelten\", \"Mehr entdecken\"] as const;\nconst groups = groupOrder\n  .map((label) => ({\n    label,\n    links: links.filter((link) => link.mobileGroup === label)\n  }))\n  .filter((group) => group.links.length > 0);\n\nconst orientationLinks = links.filter((link) => link.mobileGroup === \"Orientierung\");\nconst productLinks = links.filter((link) => link.mobileGroup === \"Produktwelten\");\nconst discoveryLinks = links.filter((link) => link.mobileGroup === \"Mehr entdecken\");\nconst comparisonLink = orientationLinks.find((link) => link.label === \"Vergleiche\");\nconst adviceLink = orientationLinks.find((link) => link.label === \"Kaufberatung\");\nconst productSectionIsCurrent = productLinks.some((link) => isCurrent(link.href));\n---\n\n<header class=\"site-header-v2\" data-site-header data-header-version=\"32.2.1\">\n  <div class=\"header-container-v2\">\n    <a href=\"/\" class=\"brand-lockup\" aria-label={`${projectName} – Startseite`}>\n      <span class=\"brand-mark\" aria-hidden=\"true\">\n        <span class=\"brand-mark__cut\"></span>\n        <span class=\"brand-mark__dot\"></span>\n      </span>\n      <span class=\"brand-name\">{projectName}</span>\n    </a>\n\n    <button\n      class=\"nav-toggle-button\"\n      type=\"button\"\n      aria-label=\"Navigation öffnen\"\n      aria-expanded=\"false\"\n      aria-controls=\"main-navigation\"\n      data-nav-toggle\n    >\n      <span class=\"nav-toggle__bars\" aria-hidden=\"true\">\n        <span></span>\n        <span></span>\n        <span></span>\n      </span>\n      <span class=\"nav-toggle__sr\">Menü</span>\n    </button>\n\n    <nav\n      id=\"main-navigation\"\n      class=\"main-nav-v2\"\n      aria-label=\"Hauptnavigation\"\n      data-main-navigation\n    >\n      <div class=\"main-nav-v2__desktop\">\n        {comparisonLink && (\n          <a\n            href={comparisonLink.href}\n            class:list={[\"main-nav-v2__desktop-link\", isCurrent(comparisonLink.href) && \"is-current\"]}\n            aria-current={isCurrent(comparisonLink.href) ? \"page\" : undefined}\n          >\n            {comparisonLink.label}\n          </a>\n        )}\n\n        {productLinks.length > 0 && (\n          <details class=\"main-nav-v2__desktop-menu\" data-desktop-menu>\n            <summary\n              class:list={[\n                \"main-nav-v2__desktop-link\",\n                \"main-nav-v2__desktop-summary\",\n                productSectionIsCurrent && \"is-current\"\n              ]}\n            >\n              <span>Produktwelten</span>\n              <svg viewBox=\"0 0 16 16\" aria-hidden=\"true\">\n                <path d=\"m4 6 4 4 4-4\" fill=\"none\" stroke=\"currentColor\" stroke-linecap=\"round\" stroke-linejoin=\"round\" stroke-width=\"1.8\"></path>\n              </svg>\n            </summary>\n\n            <div class=\"main-nav-v2__desktop-panel\">\n              {productLinks.map((link) => (\n                <a\n                  href={link.href}\n                  class:list={[\"main-nav-v2__desktop-item\", isCurrent(link.href) && \"is-current\"]}\n                  aria-current={isCurrent(link.href) ? \"page\" : undefined}\n                >\n                  <span>{link.label}</span>\n                  <svg viewBox=\"0 0 20 20\" aria-hidden=\"true\">\n                    <path d=\"m7.5 4.5 5 5.5-5 5.5\" fill=\"none\" stroke=\"currentColor\" stroke-linecap=\"round\" stroke-linejoin=\"round\" stroke-width=\"1.8\"></path>\n                  </svg>\n                </a>\n              ))}\n            </div>\n          </details>\n        )}\n\n        {adviceLink && (\n          <a\n            href={adviceLink.href}\n            class:list={[\n              \"main-nav-v2__desktop-link\",\n              \"main-nav-v2__desktop-link--emphasis\",\n              isCurrent(adviceLink.href) && \"is-current\"\n            ]}\n            aria-current={isCurrent(adviceLink.href) ? \"page\" : undefined}\n          >\n            {adviceLink.label}\n          </a>\n        )}\n\n        {discoveryLinks.map((link) => (\n          <a\n            href={link.href}\n            class:list={[\"main-nav-v2__desktop-link\", isCurrent(link.href) && \"is-current\"]}\n            aria-current={isCurrent(link.href) ? \"page\" : undefined}\n          >\n            {link.label}\n          </a>\n        ))}\n      </div>\n\n      <div class=\"main-nav-v2__mobile\">\n        {groups.map((group) => (\n          <section class=\"main-nav-v2__group\">\n            <span class=\"main-nav-v2__group-title\">{group.label}</span>\n\n            <div class=\"main-nav-v2__group-links\">\n              {group.links.map((link) => (\n                <a\n                  href={link.href}\n                  class:list={[\n                    \"main-nav-v2__mobile-link\",\n                    link.mobileEmphasis && \"main-nav-v2__mobile-link--emphasis\",\n                    isCurrent(link.href) && \"is-current\"\n                  ]}\n                  aria-current={isCurrent(link.href) ? \"page\" : undefined}\n                >\n                  <span>{link.label}</span>\n                  <svg class=\"main-nav-v2__chevron\" viewBox=\"0 0 20 20\" aria-hidden=\"true\">\n                    <path d=\"m7.5 4.5 5 5.5-5 5.5\" fill=\"none\" stroke=\"currentColor\" stroke-linecap=\"round\" stroke-linejoin=\"round\" stroke-width=\"1.8\"></path>\n                  </svg>\n                </a>\n              ))}\n            </div>\n          </section>\n        ))}\n      </div>\n    </nav>\n  </div>\n</header>\n\n<script>\n  type NavigationHeader = HTMLElement & {\n    navigationAbortController?: AbortController;\n  };\n\n  const setupHeaderNavigation = () => {\n    document.querySelectorAll<NavigationHeader>(\"[data-site-header]\").forEach((header) => {\n      header.navigationAbortController?.abort();\n\n      const button = header.querySelector<HTMLButtonElement>(\"[data-nav-toggle]\");\n      const navigation = header.querySelector<HTMLElement>(\"[data-main-navigation]\");\n      if (!button || !navigation) return;\n\n      const desktop = window.matchMedia(\"(min-width: 64rem)\");\n      const controller = new AbortController();\n      const { signal } = controller;\n      header.navigationAbortController = controller;\n\n      const closeDesktopMenus = (except?: HTMLDetailsElement) => {\n        header.querySelectorAll<HTMLDetailsElement>(\"[data-desktop-menu][open]\").forEach((menu) => {\n          if (menu !== except) menu.open = false;\n        });\n      };\n\n      const setOpen = (open: boolean, restoreFocus = false) => {\n        const mobileOpen = open && !desktop.matches;\n\n        button.setAttribute(\"aria-expanded\", String(mobileOpen));\n        button.setAttribute(\n          \"aria-label\",\n          mobileOpen ? \"Navigation schließen\" : \"Navigation öffnen\"\n        );\n        navigation.toggleAttribute(\"data-open\", mobileOpen);\n        navigation.setAttribute(\"aria-hidden\", String(!mobileOpen && !desktop.matches));\n        header.toggleAttribute(\"data-navigation-open\", mobileOpen);\n\n        if (!mobileOpen && restoreFocus) button.focus();\n      };\n\n      setOpen(false);\n\n      button.addEventListener(\"click\", (event) => {\n        event.stopPropagation();\n        setOpen(button.getAttribute(\"aria-expanded\") !== \"true\");\n      }, { signal });\n\n      navigation.addEventListener(\"click\", (event) => {\n        if ((event.target as HTMLElement).closest(\"a\")) {\n          setOpen(false);\n          closeDesktopMenus();\n        }\n      }, { signal });\n\n      header.querySelectorAll<HTMLDetailsElement>(\"[data-desktop-menu]\").forEach((menu) => {\n        menu.addEventListener(\"toggle\", () => {\n          if (menu.open) closeDesktopMenus(menu);\n        }, { signal });\n      });\n\n      document.addEventListener(\"click\", (event) => {\n        if (!header.contains(event.target as Node)) {\n          setOpen(false);\n          closeDesktopMenus();\n        }\n      }, { signal });\n\n      document.addEventListener(\"keydown\", (event) => {\n        if (event.key !== \"Escape\") return;\n\n        closeDesktopMenus();\n        if (button.getAttribute(\"aria-expanded\") === \"true\") {\n          setOpen(false, true);\n        }\n      }, { signal });\n\n      desktop.addEventListener(\"change\", () => {\n        setOpen(false);\n        closeDesktopMenus();\n      }, { signal });\n    });\n  };\n\n  const resetHeaderNavigation = () => {\n    document.querySelectorAll<NavigationHeader>(\"[data-site-header]\").forEach((header) => {\n      header.navigationAbortController?.abort();\n      header.navigationAbortController = undefined;\n\n      const button = header.querySelector<HTMLButtonElement>(\"[data-nav-toggle]\");\n      const navigation = header.querySelector<HTMLElement>(\"[data-main-navigation]\");\n\n      button?.setAttribute(\"aria-expanded\", \"false\");\n      button?.setAttribute(\"aria-label\", \"Navigation öffnen\");\n      navigation?.removeAttribute(\"data-open\");\n      header.removeAttribute(\"data-navigation-open\");\n\n      header.querySelectorAll<HTMLDetailsElement>(\"[data-desktop-menu][open]\").forEach((menu) => {\n        menu.open = false;\n      });\n    });\n  };\n\n  setupHeaderNavigation();\n  document.addEventListener(\"astro:page-load\", setupHeaderNavigation);\n  document.addEventListener(\"astro:before-swap\", resetHeaderNavigation);\n</script>\n\n<style is:global>\n  .site-header-v2 {\n    position: sticky;\n    z-index: 120;\n    top: 0;\n    border-bottom: 1px solid color-mix(in srgb, var(--pt-color-border) 78%, transparent);\n    background: color-mix(in srgb, var(--pt-color-surface) 94%, transparent);\n    backdrop-filter: blur(12px);\n    -webkit-backdrop-filter: blur(12px);\n  }\n\n  .site-header-v2 .brand-name {\n    color: var(--pt-color-text);\n  }\n\n  .site-header-v2 .header-container-v2 {\n    position: relative;\n    display: grid;\n    width: min(100%, var(--pt-content));\n    min-height: 4.25rem;\n    margin-inline: auto;\n    padding-inline: clamp(1rem, 3vw, 2rem);\n    box-sizing: border-box;\n    grid-template-columns: auto minmax(0, 1fr);\n    align-items: center;\n    gap: var(--pt-space-4);\n  }\n\n  .site-header-v2 .main-nav-v2 {\n    min-width: 0;\n    margin-left: auto;\n    border: 0;\n    background: transparent;\n    box-shadow: none;\n  }\n\n  .site-header-v2 .main-nav-v2__desktop {\n    display: flex;\n    align-items: center;\n    justify-content: flex-end;\n    gap: 0.2rem;\n  }\n\n  .site-header-v2 .main-nav-v2__desktop-link {\n    display: inline-flex;\n    min-height: 2.5rem;\n    padding: 0.55rem 0.72rem;\n    align-items: center;\n    gap: 0.35rem;\n    border: 1px solid transparent;\n    border-radius: var(--pt-radius-pill);\n    color: var(--pt-color-text-muted);\n    background: transparent;\n    font-size: 0.88rem;\n    font-weight: var(--pt-font-weight-bold);\n    line-height: 1;\n    text-decoration: none;\n    white-space: nowrap;\n    cursor: pointer;\n  }\n\n  .site-header-v2 .main-nav-v2__desktop-link:hover,\n  .site-header-v2 .main-nav-v2__desktop-link:focus-visible,\n  .site-header-v2 .main-nav-v2__desktop-link.is-current {\n    border-color: var(--pt-color-border);\n    color: var(--pt-color-text);\n    background: var(--pt-color-surface-soft);\n  }\n\n  .site-header-v2 .main-nav-v2__desktop-link--emphasis {\n    border-color: color-mix(in srgb, var(--pt-color-brand-600) 30%, var(--pt-color-border));\n    color: var(--pt-color-on-brand);\n    background: var(--pt-color-brand-600);\n  }\n\n  .site-header-v2 .main-nav-v2__desktop-link--emphasis:hover,\n  .site-header-v2 .main-nav-v2__desktop-link--emphasis:focus-visible,\n  .site-header-v2 .main-nav-v2__desktop-link--emphasis.is-current {\n    border-color: var(--pt-color-brand-700);\n    color: var(--pt-color-on-brand);\n    background: var(--pt-color-brand-700);\n  }\n\n  .site-header-v2 .main-nav-v2__desktop-menu {\n    position: relative;\n  }\n\n  .site-header-v2 .main-nav-v2__desktop-summary {\n    list-style: none;\n  }\n\n  .site-header-v2 .main-nav-v2__desktop-summary::-webkit-details-marker {\n    display: none;\n  }\n\n  .site-header-v2 .main-nav-v2__desktop-summary svg {\n    width: 0.85rem;\n  }\n\n  .site-header-v2 .main-nav-v2__desktop-menu[open] .main-nav-v2__desktop-summary svg {\n    transform: rotate(180deg);\n  }\n\n  .site-header-v2 .main-nav-v2__desktop-panel {\n    position: absolute;\n    z-index: 130;\n    top: calc(100% + 0.55rem);\n    left: 50%;\n    display: grid;\n    width: min(25rem, calc(100vw - 2rem));\n    padding: 0.55rem;\n    grid-template-columns: repeat(2, minmax(0, 1fr));\n    gap: 0.2rem;\n    border: 1px solid var(--pt-color-border);\n    border-radius: var(--pt-radius-lg);\n    color: var(--pt-color-text);\n    background: var(--pt-color-surface);\n    box-shadow: var(--pt-shadow-md);\n    transform: translateX(-50%);\n  }\n\n  .site-header-v2 .main-nav-v2__desktop-item {\n    display: grid;\n    min-width: 0;\n    min-height: 2.75rem;\n    padding: 0.6rem 0.7rem;\n    grid-template-columns: minmax(0, 1fr) auto;\n    align-items: center;\n    gap: 0.5rem;\n    border-radius: 0.65rem;\n    color: var(--pt-color-text);\n    font-size: 0.82rem;\n    font-weight: var(--pt-font-weight-bold);\n    text-decoration: none;\n  }\n\n  .site-header-v2 .main-nav-v2__desktop-item:hover,\n  .site-header-v2 .main-nav-v2__desktop-item:focus-visible,\n  .site-header-v2 .main-nav-v2__desktop-item.is-current {\n    color: var(--pt-color-brand-700);\n    background: var(--pt-color-surface-soft);\n  }\n\n  .site-header-v2 .main-nav-v2__desktop-item svg {\n    width: 0.85rem;\n  }\n\n  .site-header-v2 .main-nav-v2__mobile,\n  .site-header-v2 .nav-toggle-button {\n    display: none;\n  }\n\n  .site-header-v2 .nav-toggle__sr {\n    position: absolute;\n    inline-size: 1px;\n    block-size: 1px;\n    overflow: hidden;\n    clip-path: inset(50%);\n    white-space: nowrap;\n  }\n\n  @media (max-width: 63.99rem) {\n    .site-header-v2 .header-container-v2 {\n      width: 100%;\n      min-height: 4.25rem;\n      padding-inline: 1rem;\n      grid-template-columns: minmax(0, 1fr) auto;\n      gap: var(--pt-space-3);\n    }\n\n    .site-header-v2 .brand-lockup {\n      min-width: 0;\n    }\n\n    .site-header-v2 .nav-toggle-button {\n      position: relative;\n      display: grid;\n      inline-size: 2.9rem;\n      block-size: 2.9rem;\n      min-inline-size: 2.9rem;\n      min-block-size: 2.9rem;\n      place-items: center;\n      padding: 0;\n      border: 1px solid var(--pt-color-border);\n      border-radius: 0.85rem;\n      color: var(--pt-color-text);\n      background: var(--pt-color-surface);\n      box-shadow: var(--pt-shadow-xs);\n      cursor: pointer;\n      touch-action: manipulation;\n      appearance: none;\n    }\n\n    .site-header-v2 .nav-toggle-button::before,\n    .site-header-v2 .nav-toggle-button::after {\n      display: none;\n      content: none;\n    }\n\n    .site-header-v2 .nav-toggle-button:focus-visible {\n      outline: var(--pt-focus-ring);\n      outline-offset: var(--pt-focus-offset);\n    }\n\n    .site-header-v2 .nav-toggle__bars {\n      position: relative;\n      display: grid;\n      width: 1.25rem;\n      gap: 0.27rem;\n    }\n\n    .site-header-v2 .nav-toggle__bars > span {\n      display: block;\n      width: 100%;\n      height: 2px;\n      border-radius: var(--pt-radius-pill);\n      background: currentColor;\n      transform-origin: center;\n      transition:\n        transform var(--pt-transition-fast),\n        opacity var(--pt-transition-fast);\n    }\n\n    .site-header-v2 .nav-toggle-button[aria-expanded=\"true\"] .nav-toggle__bars > span:first-child {\n      transform: translateY(0.36rem) rotate(45deg);\n    }\n\n    .site-header-v2 .nav-toggle-button[aria-expanded=\"true\"] .nav-toggle__bars > span:nth-child(2) {\n      opacity: 0;\n    }\n\n    .site-header-v2 .nav-toggle-button[aria-expanded=\"true\"] .nav-toggle__bars > span:last-child {\n      transform: translateY(-0.36rem) rotate(-45deg);\n    }\n\n    .site-header-v2 .main-nav-v2 {\n      position: absolute;\n      z-index: 130;\n      top: calc(100% + 0.45rem);\n      right: 1rem;\n      left: 1rem;\n      max-height: calc(100dvh - 5.5rem);\n      overflow-y: auto;\n      margin: 0;\n      padding: 0.65rem;\n      border: 1px solid var(--pt-color-border);\n      border-radius: var(--pt-radius-lg);\n      color: var(--pt-color-text);\n      background: var(--pt-color-surface);\n      box-shadow: var(--pt-shadow-lg);\n      visibility: hidden;\n      opacity: 0;\n      pointer-events: none;\n      transform: translateY(-0.3rem);\n      overscroll-behavior: contain;\n      transition:\n        opacity 120ms ease-out,\n        transform 120ms ease-out,\n        visibility 0s linear 120ms;\n    }\n\n    .site-header-v2 .main-nav-v2[data-open] {\n      visibility: visible;\n      opacity: 1;\n      pointer-events: auto;\n      transform: translateY(0);\n      transition-delay: 0s;\n    }\n\n    .site-header-v2 .main-nav-v2__desktop {\n      display: none;\n    }\n\n    .site-header-v2 .main-nav-v2__mobile {\n      display: grid;\n      gap: 0.65rem;\n    }\n\n    .site-header-v2 .main-nav-v2__group {\n      display: grid;\n      gap: 0.2rem;\n    }\n\n    .site-header-v2 .main-nav-v2__group + .main-nav-v2__group {\n      padding-top: 0.55rem;\n      border-top: 1px solid var(--pt-color-border);\n    }\n\n    .site-header-v2 .main-nav-v2__group-title {\n      padding: 0.2rem 0.55rem;\n      color: var(--pt-color-text-muted);\n      font-size: 0.66rem;\n      font-weight: var(--pt-font-weight-black);\n      letter-spacing: 0.09em;\n      text-transform: uppercase;\n    }\n\n    .site-header-v2 .main-nav-v2__group-links {\n      display: grid;\n      gap: 0.05rem;\n    }\n\n    .site-header-v2 .main-nav-v2__mobile-link {\n      display: grid;\n      min-width: 0;\n      min-height: 2.8rem;\n      padding: 0.58rem 0.65rem;\n      grid-template-columns: minmax(0, 1fr) auto;\n      align-items: center;\n      gap: 0.7rem;\n      border: 1px solid transparent;\n      border-radius: 0.7rem;\n      color: var(--pt-color-text);\n      background: transparent;\n      font-size: 0.9rem;\n      font-weight: var(--pt-font-weight-bold);\n      line-height: 1.25;\n      text-decoration: none;\n    }\n\n    .site-header-v2 .main-nav-v2__mobile-link:hover,\n    .site-header-v2 .main-nav-v2__mobile-link:focus-visible,\n    .site-header-v2 .main-nav-v2__mobile-link.is-current {\n      border-color: var(--pt-color-border);\n      color: var(--pt-color-brand-700);\n      background: var(--pt-color-surface-soft);\n    }\n\n    .site-header-v2 .main-nav-v2__mobile-link--emphasis {\n      color: var(--pt-color-on-brand);\n      background: var(--pt-color-brand-600);\n    }\n\n    .site-header-v2 .main-nav-v2__mobile-link--emphasis:hover,\n    .site-header-v2 .main-nav-v2__mobile-link--emphasis:focus-visible,\n    .site-header-v2 .main-nav-v2__mobile-link--emphasis.is-current {\n      border-color: var(--pt-color-brand-700);\n      color: var(--pt-color-on-brand);\n      background: var(--pt-color-brand-700);\n    }\n\n    .site-header-v2 .main-nav-v2__chevron {\n      width: 0.95rem;\n      color: currentColor;\n    }\n  }\n\n  @media (min-width: 64rem) {\n    .site-header-v2 .nav-toggle-button {\n      display: none;\n      visibility: hidden;\n      pointer-events: none;\n    }\n\n    .site-header-v2 .main-nav-v2 {\n      position: static;\n      display: block;\n      visibility: visible;\n      opacity: 1;\n      pointer-events: auto;\n      transform: none;\n    }\n\n    .site-header-v2 .main-nav-v2__desktop {\n      display: flex;\n    }\n\n    .site-header-v2 .main-nav-v2__mobile {\n      display: none;\n    }\n  }\n\n  @media (prefers-reduced-motion: reduce) {\n    .site-header-v2 .main-nav-v2,\n    .site-header-v2 .nav-toggle__bars > span {\n      transition-duration: 0.01ms;\n    }\n  }\n</style>\n",
  "packages/affiliate-core/src/components/Footer.astro": "---\nimport { projectConfig } from \"@app/project.config\";\n\nconst year = new Date().getFullYear();\nconst productWorldLinks = projectConfig.headerLinks\n  .filter((link) => link.mobileGroup === \"Produktwelten\")\n  .map((link) => ({\n    label: link.label,\n    href: link.href\n  }));\n\nconst footerColumns = [\n  {\n    title: \"Produktwelten\",\n    links: productWorldLinks,\n    productWorlds: true\n  },\n  ...projectConfig.footer.columns.map((column) => ({\n    ...column,\n    productWorlds: false\n  }))\n];\n\nconst footerColumnStyle = `--footer-column-count: ${footerColumns.length}`;\n---\n\n<footer class=\"footer-v2\" data-footer-version=\"32.2.1\">\n  <div class=\"footer-inner-v2\">\n    <section class=\"footer-brand-v2\" aria-labelledby=\"footer-brand-title\">\n      <a class=\"footer-brand-lockup\" href=\"/\" aria-label={`${projectConfig.projectName} – Startseite`}>\n        <span class=\"brand-mark\" aria-hidden=\"true\">\n          <span class=\"brand-mark__cut\"></span>\n          <span class=\"brand-mark__dot\"></span>\n        </span>\n        <span class=\"footer-brand-name\" id=\"footer-brand-title\">\n          {projectConfig.projectName}\n        </span>\n      </a>\n\n      <p>{projectConfig.footer.description}</p>\n    </section>\n\n    <div class=\"footer-main-v2\" style={footerColumnStyle}>\n      {footerColumns.map((column, index) => {\n        const headingId = `footer-column-${index}`;\n\n        return (\n          <nav\n            class:list={[\n              \"footer-column-v2\",\n              column.productWorlds && \"footer-column-v2--products\"\n            ]}\n            aria-labelledby={headingId}\n          >\n            <h2 id={headingId}>{column.title}</h2>\n            <div class=\"footer-column-links-v2\">\n              {column.links.map((link) => (\n                <a href={link.href}>{link.label}</a>\n              ))}\n            </div>\n          </nav>\n        );\n      })}\n    </div>\n\n    <div class=\"footer-bottom-v2\">\n      <p class=\"footer-copyright-v2\">\n        © {year} {projectConfig.projectName}\n      </p>\n\n      <p class=\"footer-transparency-v2\">\n        {projectConfig.footer.transparency}\n      </p>\n    </div>\n  </div>\n</footer>\n\n<style is:global>\n  /* Footer.astro is the sole owner of footer presentation. */\n\n  .footer-v2 {\n    margin-top: clamp(4rem, 8vw, 6rem);\n    padding: 2.75rem clamp(1rem, 4vw, 2rem) 1.5rem;\n    border-top: 1px solid color-mix(in srgb, var(--pt-color-on-brand-surface, #ffffff) 10%, transparent);\n    background: var(--pt-color-brand-surface, #102019);\n  }\n\n  .footer-inner-v2 {\n    width: min(100%, 1200px);\n    margin-inline: auto;\n  }\n\n  .footer-brand-v2 {\n    display: flex;\n    padding-bottom: 1.75rem;\n    align-items: center;\n    justify-content: space-between;\n    gap: 2rem;\n    border-bottom: 1px solid color-mix(in srgb, var(--pt-color-on-brand-surface, #ffffff) 12%, transparent);\n  }\n\n  .footer-brand-lockup {\n    display: inline-flex;\n    flex: 0 0 auto;\n    align-items: center;\n    gap: 0.65rem;\n    color: var(--pt-color-on-brand-surface, #ffffff);\n    text-decoration: none;\n  }\n\n  .footer-brand-name {\n    color: var(--pt-color-on-brand-surface, #ffffff);\n    font-size: 1.35rem;\n    font-weight: 950;\n    letter-spacing: -0.04em;\n  }\n\n  .footer-brand-v2 p {\n    max-width: 42rem;\n    margin: 0;\n    color: var(--pt-color-on-brand-surface-muted, rgb(255 255 255 / 0.72));\n    font-size: 0.9rem;\n    line-height: 1.6;\n  }\n\n  .footer-main-v2 {\n    display: grid;\n    padding: 2rem 0;\n    grid-template-columns: repeat(var(--footer-column-count, 4), minmax(0, 1fr));\n    gap: clamp(1.25rem, 3vw, 3rem);\n  }\n\n  .footer-column-v2 h2 {\n    margin: 0 0 0.75rem;\n    color: var(--pt-color-on-brand-surface-accent, #86efac);\n    font-size: 0.68rem;\n    font-weight: 950;\n    letter-spacing: 0.11em;\n    text-transform: uppercase;\n  }\n\n  .footer-column-links-v2 {\n    display: grid;\n    gap: 0.48rem;\n  }\n\n  .footer-column-v2 a {\n    width: fit-content;\n    color: var(--pt-color-on-brand-surface-muted, rgb(255 255 255 / 0.72));\n    font-size: 0.82rem;\n    font-weight: 700;\n    line-height: 1.35;\n    text-decoration: none;\n  }\n\n  .footer-column-v2 a:hover {\n    color: var(--pt-color-on-brand-surface, #ffffff);\n  }\n\n  .footer-column-v2 a:focus-visible {\n    border-radius: 4px;\n    outline: 2px solid var(--pt-color-brand-300, #86efac);\n    outline-offset: 4px;\n  }\n\n  .footer-bottom-v2 {\n    display: flex;\n    padding-top: 1.25rem;\n    align-items: flex-start;\n    justify-content: space-between;\n    gap: 2rem;\n    border-top: 1px solid color-mix(in srgb, var(--pt-color-on-brand-surface, #ffffff) 12%, transparent);\n  }\n\n  .footer-bottom-v2 p {\n    margin: 0;\n  }\n\n  .footer-copyright-v2 {\n    flex: 0 0 auto;\n    color: var(--pt-color-on-brand-surface, #ffffff);\n    font-size: 0.78rem;\n    font-weight: 800;\n  }\n\n  .footer-transparency-v2 {\n    max-width: 50rem;\n    color: var(--pt-color-on-brand-surface-muted, rgb(255 255 255 / 0.72));\n    font-size: 0.74rem;\n    line-height: 1.55;\n  }\n\n  @media (max-width: 760px) {\n    .footer-v2 {\n      padding-top: 2.25rem;\n    }\n\n    .footer-brand-v2 {\n      display: grid;\n      gap: 0.85rem;\n    }\n\n    .footer-main-v2 {\n      grid-template-columns: repeat(2, minmax(0, 1fr));\n      gap: 1.75rem 1.25rem;\n    }\n\n    .footer-column-v2--products {\n      grid-column: 1 / -1;\n    }\n\n    .footer-column-v2--products .footer-column-links-v2 {\n      grid-template-columns: repeat(2, minmax(0, 1fr));\n      column-gap: 1rem;\n    }\n\n    .footer-bottom-v2 {\n      display: grid;\n      gap: 0.65rem;\n    }\n  }\n\n  @media (max-width: 520px) {\n    .footer-main-v2 {\n      column-gap: 1rem;\n    }\n\n    .footer-column-v2 a {\n      font-size: 0.78rem;\n    }\n  }\n</style>\n",
  "apps/pfotentechnik/test/navigation-footer-compact-repair-32.2.1.test.mjs": "import test from \"node:test\";\nimport assert from \"node:assert/strict\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport { fileURLToPath } from \"node:url\";\n\nconst app = path.resolve(path.dirname(fileURLToPath(import.meta.url)), \"..\");\nconst root = path.resolve(app, \"../..\");\n\nconst headerPath = path.join(root, \"packages/affiliate-core/src/components/Header.astro\");\nconst footerPath = path.join(root, \"packages/affiliate-core/src/components/Footer.astro\");\nconst configPath = path.join(app, \"src/project.config.ts\");\n\nconst header = fs.readFileSync(headerPath, \"utf8\");\nconst footer = fs.readFileSync(footerPath, \"utf8\");\nconst config = fs.readFileSync(configPath, \"utf8\");\n\ntest(\"mobile navigation is a compact dropdown instead of a full-screen layer\", () => {\n  assert.match(header, /data-header-version=\"32\\.2\\.1\"/);\n  assert.match(\n    header,\n    /@media\\s*\\(max-width:\\s*63\\.99rem\\)[\\s\\S]*?\\.site-header-v2 \\.main-nav-v2\\s*\\{[\\s\\S]*?position:\\s*absolute/\n  );\n  assert.doesNotMatch(\n    header,\n    /@media\\s*\\(max-width:\\s*63\\.99rem\\)[\\s\\S]*?\\.site-header-v2 \\.main-nav-v2\\s*\\{[\\s\\S]*?position:\\s*fixed/\n  );\n  assert.doesNotMatch(header, /inset:\\s*0/);\n  assert.doesNotMatch(header, /main-nav-v2__mobile-head/);\n  assert.doesNotMatch(header, /main-nav-v2__mobile-shell/);\n});\n\ntest(\"mobile links stay compact and do not repeat descriptions or code badges\", () => {\n  assert.match(header, /min-height:\\s*2\\.8rem/);\n  assert.doesNotMatch(header, /main-nav-v2__mobile-copy/);\n  assert.doesNotMatch(header, /main-nav-v2__code/);\n  assert.doesNotMatch(header, /\\{link\\.description && <small>/);\n});\n\ntest(\"burger lifecycle is reinitialized after Astro navigation\", () => {\n  assert.match(header, /header\\.navigationAbortController\\?\\.abort\\(\\)/);\n  assert.match(header, /document\\.addEventListener\\(\"astro:page-load\", setupHeaderNavigation\\)/);\n  assert.match(header, /document\\.addEventListener\\(\"astro:before-swap\", resetHeaderNavigation\\)/);\n  assert.doesNotMatch(header, /requestAnimationFrame\\([\\s\\S]*?\\.focus\\(\\)/);\n  assert.doesNotMatch(header, /pt-navigation-open/);\n});\n\ntest(\"footer is a compact link footer without landing-page blocks\", () => {\n  assert.match(footer, /data-footer-version=\"32\\.2\\.1\"/);\n  assert.match(footer, /footerColumns = \\[/);\n  assert.match(footer, /footer-column-v2--products/);\n  assert.doesNotMatch(footer, /footer-top-v3/);\n  assert.doesNotMatch(footer, /footer-actions-v3/);\n  assert.doesNotMatch(footer, /footer-product-grid-v3/);\n  assert.doesNotMatch(footer, /footer-values/);\n});\n\ntest(\"all new product worlds remain available from the shared navigation data\", () => {\n  for (const label of [\n    \"Futterautomaten\",\n    \"Trinkbrunnen\",\n    \"GPS-Tracker\",\n    \"Katzenklappen\",\n    \"Haustierkameras\",\n    \"Automatische Katzentoiletten\"\n  ]) {\n    assert.match(config, new RegExp(`label:\\\\s*\"${label.replace(/[.*+?^${}()|[\\]\\\\]/g, \"\\\\$&\")}\"`));\n  }\n});\n\ntest(\"repair does not introduce important overrides\", () => {\n  assert.doesNotMatch(header, /!important/);\n  assert.doesNotMatch(footer, /!important/);\n});\n"
};
const backupRoot = path.join(
  root,
  ".patch-backups",
  `${PATCH}-${new Date().toISOString().replace(/[:.]/g, "-")}`
);

let backupCreated = false;
let changed = 0;

const ensureParent = (file) => fs.mkdirSync(path.dirname(file), { recursive: true });

const backup = (relativePath, target) => {
  if (!fs.existsSync(target)) return;
  const destination = path.join(backupRoot, relativePath);
  ensureParent(destination);
  fs.copyFileSync(target, destination);
  backupCreated = true;
};

const writeAtomic = (target, content) => {
  ensureParent(target);
  const temporary = `${target}.${process.pid}.tmp`;
  fs.writeFileSync(temporary, content, "utf8");
  fs.renameSync(temporary, target);
};

for (const [relativePath, content] of Object.entries(payloads)) {
  const target = path.join(root, relativePath);
  const current = fs.existsSync(target) ? fs.readFileSync(target, "utf8") : null;

  if (current === content) {
    console.log(`[${PATCH}] Unverändert: ${relativePath}`);
    continue;
  }

  backup(relativePath, target);
  writeAtomic(target, content);
  changed += 1;
  console.log(`[${PATCH}] Geschrieben: ${relativePath}`);
}

if (!backupCreated && fs.existsSync(backupRoot)) {
  fs.rmSync(backupRoot, { recursive: true, force: true });
}

const run = (command, args) => {
  console.log(`[${PATCH}] ${command} ${args.join(" ")}`);
  const result = spawnSync(command, args, {
    cwd: root,
    stdio: "inherit",
    shell: process.platform === "win32"
  });

  if (result.error) throw result.error;
  if (result.status !== 0) {
    throw new Error(
      `[${PATCH}] Kommando fehlgeschlagen (${result.status}): ${command} ${args.join(" ")}`
    );
  }
};

run(process.execPath, [
  "--test",
  "apps/pfotentechnik/test/navigation-footer-compact-repair-32.2.1.test.mjs",
  "apps/pfotentechnik/test/footer-style-ownership-cleanup-26.0.5.test.mjs"
]);

if (!skipBuild) {
  run("npm", ["--workspace", "apps/pfotentechnik", "run", "build"]);
}

console.log(
  `[${PATCH}] Fertig. ${changed} Datei(en) geändert.${skipBuild ? " Build übersprungen." : ""}`
);
